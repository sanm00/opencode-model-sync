# Contributing

1. Fork and clone the repository.
2. Run `npm install`.
3. Make focused changes and add tests for behavior changes.
4. Run `npm run check` before opening a pull request.

Use conventional, concise commit messages. Do not include provider credentials,
real status files, or generated `dist` files in pull requests.
